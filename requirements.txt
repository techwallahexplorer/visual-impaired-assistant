# ──────────────────────────────────────────────────────────
# visual-impaired-assistant — pinned production dependencies
# [FIX S4] All versions pinned to exact releases to prevent
#          supply-chain attacks via malicious patch releases.
# ──────────────────────────────────────────────────────────

SpeechRecognition==3.10.4
PyAudio==0.2.14
pyttsx3==2.90
nltk==3.8.1
colorama==0.4.6

# [FIX A4] googletrans 3.1.0a0 is an unstable alpha that calls
# Google's internal (unofficial) endpoint and breaks without notice.
# Pinned to the last stable release. For production, replace with
# the official google-cloud-translate SDK and a service account key.
googletrans==4.0.0rc1

# Optional — only needed if Firebase logging is enabled via
# GOOGLE_APPLICATION_CREDENTIALS env var.
firebase-admin==6.5.0

# Indic NLP — pinned to avoid silent breakage on minor bumps.
indic-nlp-library==0.91
